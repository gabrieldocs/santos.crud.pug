html
    include includes/head.pug
body
include includes/nav.pug
.container.mt-5
    .row.d-flex.justify-content-center 
        .col-md-4   
            center 
                h1.text-white.mb-5
                    i.fab.fa-spotify.text-white
                    span.ml-3 ATUALIZA ITEM          
            for post in posts 
                form(action = "/update" method = "post")
                    input.form-control.mb-3(type='text', placeholder='Título' name='titulo', value = post.titulo)
                    input.form-control.mb-3(type='text', placeholder='Conteúdo...' name='conteudo' value = post.conteudo)
                    input(type="hidden" name = "id" value = post.id)
                    button.btn.btn-spotify.btn-block(type="submit") Atualizar